# 游戏体验优化与翻译问题报告

**测试日期**: 2026-03-19  
**游戏版本**: v0.7.0  
**测试方式**: Playwright E2E 手动探索  
**测试时长**: ~15 分钟  
**覆盖范围**: 全部 11 个标签页 + 核心交互功能

---

## 📋 翻译问题报告

### 🔴 严重问题（显示错误）

| 位置 | 当前内容 | 期望内容 | 说明 |
|------|----------|----------|------|
| `index.html:87` | `<span class="resource-icon">ite</span>` (铁矿) | `<span class="resource-icon">⛏️</span>` | **明显的 HTML 构建错误** - "ite" 是文本而非图标 |
| `index.html:127` | `<span class="resource-icon">ite</span>` (铁锭) | `<span class="resource-icon">🔩</span>` | 同上，第二处相同错误 |

### 🟡 中等问题（混合中英文/格式不统一）

| 位置 | 当前内容 | 建议修改 | 说明 |
|------|----------|----------|------|
| `index.html:251` | `浅色 (Light)` | `浅色主题 / Light Theme` | 与英文保持一致性 |
| `index.html:252` | `深色 (Dark)` | `深色主题 / Dark Theme` | 同上 |
| `index.html:259` | `English` | `英语 / English` | 语言选项应双语显示 |
| `index.html:277,280,283` | `BASE64` | `Base64` 或保留大写但添加提示 | 技术术语大小写统一 |
| `js/i18n.js:223-224` | `lightTheme/darkTheme` | 需要补充中文翻译 | 英文翻译不完整 |

### 🟢 轻微问题（可优化）

| 位置 | 当前内容 | 建议 |
|------|----------|------|
| `index.html:6` | meta description 为中文 | 考虑添加双语或使用 i18n |
| `index.html:8` | title 为中文 | 同上 |
| `index.html:295` | footer 文字硬编码 | 通过 i18n 渲染 |
| `i18n.js` | 缺少 Tab 名称的英文翻译 | resources, technology, work, housing, statistics, unlocks 等 |
| `i18n.js` | 缺少资源分类标题翻译 | 初级资源、次级资源、高级资源 |

### 🔴 新增：语言切换后部分文本未更新

**发现时间**: 2026-03-19 13:34  
**影响范围**: 设置面板、科技树等动态内容

#### 1. 设置面板静态文本

| 元素 ID | 当前 (英文模式) | 应该是什么 |
|---------|-----------------|------------|
| `theme-select-setting` label | `Theme / Theme` | `Theme / 主题` |
| `language-select-setting` label | `Language / Language` | `Language / 语言` |
| `game-version` text | `游戏版本：v0.7.0` | `Game Version: v0.7.0` |
| `save-load-title` heading | `保存/加载 Save` | `Save/Load Game` |
| theme options | `浅色 (Light)`, `深色 (Dark)` | `Light Theme`, `Dark Theme` |
| language option | `简体中文` | `Simplified Chinese` |

#### 2. 科技树混合翻译（严重）

**发现时间**: 2026-03-19 13:35  
**影响范围**: TechnologyManager 渲染的科技卡片

| 位置 | 当前 (英文模式) | 应该是什么 |
|------|-----------------|------------|
| 科技名称 | `高级采矿`, `基础采石` 等 | `Advanced Mining`, `Basic Quarry` 等 |
| 效果描述 | `+20% Iron Ore 产量` | `+20% Iron Ore Production` |
| 效果描述 | `+15% Crystal 产量` | `+15% Crystal Production` |
| 效果描述 | `🏗️ 解锁：Factory` | `🏗️ Unlocks: Factory` |

**根本原因**: 
- `TechnologyManager` 直接读取 Rust 返回的中文科技名称
- JS 层未对科技数据进行 i18n 转换
- `i18n.js` 缺少科技名称和效果的翻译映射

**文件索引**:
- `js/technology-manager.js` - 科技渲染逻辑（约 1000+ 行）
- `js/i18n.js:606-625` - updateAllTranslations() 未覆盖科技数据
- `src/entities/technology.rs` - Rust 端科技定义（可能硬编码中文）
- `src/systems/tech.rs` - 科技系统实现

**影响评估**: 
- **严重性**: 🔴 高 - 英文模式下核心游戏内容无法理解
- **涉及范围**: 36 项科技全部受影响
- **用户体验**: 切换到英文后几乎无法阅读科技信息

---

### 📝 详细问题清单 - 科技树翻译缺失

**发现时间**: 2026-03-19 13:35  
**测试方法**: 切换到英文模式 → 查看科技面板 → 搜索过滤功能

#### 问题示例（从实际页面捕获）

| 科技名称 (当前) | 效果描述 (当前) | 应该是什么 |
|-----------------|-----------------|------------|
| 高级采矿 | +20% Iron Ore 产量 | Advanced Mining<br>+20% Iron Ore Production |
| 高级采石 | +15% Crystal 产量 | Advanced Quarrying<br>+15% Crystal Production |
| 高级伐木 | +25% Wood 产量 | Advanced Logging<br>+25% Wood Production |
| 基础采矿 | +10% Coal 产量 | Basic Mining<br>+10% Coal Production |
| 基础工程 | 🏗️ 解锁：Factory | Basic Engineering<br>🏗️ Unlocks: Factory |

#### 受影响的翻译键（需要在 i18n.js 中添加）

```javascript
// 需要添加的英文科技名称和效果
'tech_AdvancedMining': 'Advanced Mining',
'tech_BasicQuarrying': 'Basic Quarrying',
// ... 所有 36 项科技

'techEffect_Production': 'Production',
'techEffect_Unlocks': 'Unlocks',
```

#### 可能的修复路径

**路径 A: Rust 端国际化**（推荐）
- 在 Rust 结构体中添加多语言字段
- WASM API 返回当前语言的文本
- 优点：一次性解决，性能最好

**路径 B: JS 端映射转换**
- TechnologyManager 读取中文键值
- 通过 i18n.t(key) 转换为当前语言
- 优点：实现简单，无需修改 Rust

**路径 C: 混合方案**
- 核心术语（资源名、建筑名）由 Rust 提供
- 描述性文本（效果说明）由 JS 本地化
- 平衡性能和可维护性

---

### 🔧 JavaScript Console Errors

**发现时间**: 2026-03-19 13:33  
**错误信息**: `TypeError: Cannot set properties of null (setting 'value')`  
**位置**: `index.html:435:74`

```javascript
// 代码尝试访问不存在的元素
document.getElementById('language-select').value = selectedLanguage;
//                                    ^^^^^^^^^^^^^^^^
//                                    该元素在 HTML 中不存在
```

**影响**: 
- 语言切换时控制台报错
- 不影响核心功能但降低专业度
- 可能导致后续逻辑执行失败

**修复建议**:
```javascript
// 方案 1: 删除对不存在元素的引用
if (document.getElementById('language-select')) {
    document.getElementById('language-select').value = selectedLanguage;
}

// 方案 2: 在 header 中添加缺失的元素（如果设计需要）
```

---

### 🎨 新增：资源分类标签未翻译

**发现时间**: 2026-03-19 13:36  
**影响范围**: 资源面板的分类标签

| 当前 (英文模式) | 应该是什么 |
|-----------------|------------|
| `初级资源` | `Primary Resources` |
| `次级资源` | `Secondary Resources` |
| `高级资源` | `Advanced Resources` |

**位置**: `index.html:62-65` - 硬编码在 HTML 中

---

### 🔴 新增：资源分类系统不一致（严重）

**发现时间**: 2026-03-19 13:42  
**影响范围**: `js/resource-panel.js` vs `index.html`

#### 问题描述

**ResourcePanel.js 定义的分类** (Line 21-27):
```javascript
const categories = [
  { id: 'ALL', label: '全部资源' },
  { id: 'TIER1_BASIC', label: '基础资源' },
  { id: 'TIER2_PROCESSED', label: '加工材料' },
  { id: 'TIER3_ADVANCED', label: '高科技' },
  { id: 'SPECIAL', label: '特殊' }
];
```

**Index.html 中定义的分类** (Line 62-65):
```html
<button class="category-tab-button" data-category="primary">初级资源</button>
<button class="category-tab-button" data-category="secondary">次级资源</button>
<button class="category-tab-button" data-category="advanced">高级资源</button>
```

#### 根本原因
- **两套不同的分类系统并存**，互不兼容
- `resource-panel.js` 是较新的实现（5 个分类）
- `index.html` 保留的是旧版结构（3 个分类）
- 导致页面显示混乱，功能可能无法正常运作

#### 影响评估
- **严重性**: 🔴 高 - 核心 UI 功能失效
- **用户可见**: 点击"次级资源"后看不到"高级资源"选项
- **代码质量**: 维护困难，逻辑分散

#### 修复建议

**方案 A: 统一为新系统**（推荐）
```html
<!-- 修改 index.html 中的资源分类 -->
<button class="category-tab-button" data-tier="ALL">全部资源</button>
<button class="category-tab-button" data-tier="TIER1_BASIC">基础资源</button>
<button class="category-tab-button" data-tier="TIER2_PROCESSED">加工材料</button>
<button class="category-tab-button" data-tier="TIER3_ADVANCED">高科技</button>
<button class="category-tab-button" data-tier="SPECIAL">特殊</button>
```

**方案 B: 回退到旧系统**
- 修改 `resource-panel.js` 使用 primary/secondary/advanced 分类
- 删除 TIER1/TIER2/TIER3/SPECIAL 相关逻辑

**需要确认**:
- 哪套系统是预期使用的？
- `resource-classification.js` 中的分类定义是否匹配？

#### 代码现状分析

| 文件 | 分类系统 | 状态 |
|------|----------|------|
| `js/resource-classification.js` | TIER1_BASIC, TIER2_PROCESSED, TIER3_ADVANCED, SPECIAL | ✅ 完整实现 (356 行) |
| `js/resource-panel.js` | ALL, TIER1_BASIC, TIER2_PROCESSED, TIER3_ADVANCED, SPECIAL | ✅ 新 UI 实现 |
| `js/unlocks.js:187-189` | primary, secondary, advanced | ⚠️ 旧代码残留 |
| `index.html:62-65` | primary, secondary, advanced | ⚠️ 旧 HTML 结构 |

#### 影响范围
- **资源显示逻辑分裂**: 两套系统并行，可能导致数据不同步
- **UI 不一致**: 用户看到的标签与底层逻辑不匹配
- **维护困难**: 修改一处可能破坏另一处

#### 建议修复步骤
1. **确定目标系统**: 推荐使用新的 TIER1/TIER2/TIER3/SPECIAL 系统
2. **统一 HTML 结构**: 修改 `index.html` 使用新的 data-tier 属性
3. **清理旧代码**: 删除 `unlocks.js` 中过时的 primary/secondary 引用
4. **添加 i18n 支持**: 为所有分类标签添加中英文翻译
5. **测试验证**: 确保所有资源正确归类显示

---

### 🎯 新增：成就系统部分翻译缺失

**发现时间**: 2026-03-19 13:43  
**影响范围**: AchievementsManager 渲染的成就卡片

#### 观察结果

| 元素 | 当前 (英文模式) | 状态 |
|------|-----------------|------|
| 分类标题 | `Clicks`, `Resources`, `Buildings`, `Industry`, `Unlocks` | ✅ 已翻译 |
| 成就名称 | `点击新手`, `第一桶金`, `建筑爱好者` | ❌ 未翻译 |
| 成就描述 | `点击 10 次`, `购买第一座建筑` | ❌ 未翻译 |
| 解锁时间 | `6 minutes ago`, `9 minutes ago` | ✅ 已翻译 |

#### 问题示例

```yaml
# 成就卡片 - 中英混合
heading "Clicks" [level=4]           # ✅ 分类标题已翻译
  generic [ref=e3314]: 点击新手       # ❌ 成就名称是中文
  generic [ref=e3315]: 点击 10 次      # ❌ 成就是描述是中文
```

#### 根本原因
- `achievements.js` 直接读取 Rust 返回的成就数据
- Rust 端的成就名称和描述硬编码为中文
- JS 层没有对成就数据进行 i18n 转换

#### 修复建议
同科技树问题：
- **方案 A**: 在 Rust 端添加多语言字段
- **方案 B**: 在 `achievements.js` 中添加映射表
- **方案 C**: 通过 i18n 键值引用（推荐）

```javascript
// achievements.js 示例
const achievementNames = {
  'click_10': { zh: '点击新手', en: 'Click Novice' },
  'first_coin_100': { zh: '第一桶金', en: 'First Pot of Gold' }
};

element.textContent = window.i18n.t(`achievement_${id}`);
```

---

### 🎯 新增：解锁页面未国际化

**发现时间**: 2026-03-19 13:44  
**影响范围**: UnlockManager 渲染的解锁面板

#### 观察结果

| 元素 | 当前 (英文模式) | 状态 |
|------|-----------------|------|
| 阶段标题 | `当前阶段 / ACTIVE DOSSIER` | ⚠️ 中英混合 |
| 阶段名称 | `蛆虫阶段` | ❌ 未翻译 |
| 阶段提示 | `PHASE 03` | ✅ 已翻译 |
| 描述文本 | 全部为中文 | ❌ 未翻译 |
| 进度标签 | `黑暗科技准备度`, `蛆虫影响` | ❌ 未翻译 |

#### 问题示例

```yaml
generic [ref=e3475]: 当前阶段 / ACTIVE DOSSIER   # ⚠️ 部分翻译
generic [ref=e3476]: 蛆虫阶段                      # ❌ 完全中文
generic [ref=e3478]: 尸体腐化揭示黑暗生物生产链。   # ❌ 完全中文
```

#### 根本原因
- `unlocks.js` 直接读取 Rust 返回的解锁数据
- Rust 端的解锁描述硬编码为中文
- JS 层没有对解锁数据进行 i18n 转换

#### 修复建议
同科技树和成就系统：
- **方案 A**: 在 Rust 端添加多语言字段（推荐）
- **方案 B**: 在 `unlocks.js` 中添加映射表
- **方案 C**: 通过 i18n 键值引用

#### 涉及文件
- `js/unlocks.js` - 约 540 行，需要添加 i18n 支持
- `src/systems/unlock.rs` - Rust 端解锁系统定义

---

### 🎯 新增：设置面板多处未翻译

**发现时间**: 2026-03-19 13:45  
**影响范围**: Settings 标签页

#### 观察结果 (英文模式下)

| 元素 | 当前显示 | 应该是什么 |
|------|----------|------------|
| 主题标签 | `Theme / Theme` | `Theme / 主题` |
| 语言标签 | `Language / Language` | `Language / 语言` |
| 游戏版本 | `游戏版本：v0.7.0` | `Game Version: v0.7.0` |
| 重置按钮 | `重置游戏` | `Reset Game` |
| 保存标题 | `保存/加载 Save` | `Save/Load Game` |
| 手动保存 | `手动保存` | `Manual Save` |
| 导出按钮 | `导出为 BASE64` | `Export to BASE64` |
| 输入框占位符 | `在此粘贴 BASE64 字符串...` | `Paste BASE64 string here...` |
| 导入按钮 | `从 BASE64 导入` | `Import from BASE64` |
| 主题选项 | `浅色 (Light)`, `深色 (Dark)` | `Light Theme`, `Dark Theme` |
| 语言选项 | `简体中文` | `Simplified Chinese` |

#### 根本原因
- `index.html:441-482` 中的设置面板 HTML 硬编码了中文文本
- 事件监听器在第 445 行使用了中文的 `confirm()` 对话框
- 没有通过 `i18n.t()` 获取翻译文本

#### 修复建议
```javascript
// index.html:445
resetButton.addEventListener('click', function() {
    if (confirm(window.i18n.t('resetGameConfirm'))) {
        // ...
    }
});

// 更新所有按钮和标签的 textContent
document.getElementById('reset-game').textContent = window.i18n.t('resetGame');
```

---

## ✅ 游戏亮点总结

### 设计特色
- **黑暗主题设定**: 秩序衰败、蛆虫转化等独特世界观
- **完整的科技树**: 36 项科技分级清晰（T1-T4）
- **成就系统**: 分类详细（点击、资源、建筑、工业、解锁）
- **生命周期管理**: 工人饥饿→尸体→蛆虫形成独特循环
- **解锁机制**: 渐进式揭示系统，避免信息过载

### 功能完整性
| 功能 | 状态 | 备注 |
|------|------|------|
| 资源管理系统 | ✅ | 60+ 种资源，分三级展示 |
| 建筑购买与升级 | ✅ | 自动累加价格，购买成功触发成就 |
| 科技研究与解锁 | ✅ | 36 项科技，搜索/过滤功能完善 |
| 成就追踪系统 | ✅ | 分类清晰，进度实时显示 |
| 数据统计面板 | ✅ | 完整的游戏统计数据 |
| 存档/导入导出 | ✅ | BASE64 格式，剪贴板复制成功 |
| 多主题切换 | ✅ | 深色/浅色模式正常工作 |
| 多语言支持 | ⚠️ | 核心文本已翻译，设置面板部分未更新 |

### 交互测试验证
- ✅ 金币点击赚取功能正常
- ✅ 建筑购买流程完整（花费扣除→数量增加→价格累加）
- ✅ 成就解锁弹窗及时触发
- ✅ 自动保存每 15 秒执行一次
- ✅ 手动保存功能正常
- ✅ BASE64 导出并复制到剪贴板成功
- ✅ 语言切换后主要文本正确更新（标题、副标题、资源名称等）
- ✅ 主题切换生效（深色/浅色模式）

### 技术实现
- ✅ Rust + WASM 核心逻辑
- ✅ Manager 模式 UI 架构
- ✅ i18n 国际化系统
- ✅ NumberFormatter 数字格式化
- ✅ 自动保存机制

---

## 📊 问题统计

| 类别 | 数量 | 优先级 | 状态 |
|------|------|--------|------|
| HTML 图标错误 | 2 | 🔴 高 | 待修复 |
| **资源分类系统不一致** | **1** | 🔴 高 | **紧急** |
| **设置面板多处未翻译** | **10+** | 🔴 高 | **紧急** |
| **科技树混合翻译** | **36+** | 🔴 高 | **紧急** |
| **成就系统部分翻译** | **20+** | 🟡 中 | 待修复 |
| **解锁页面未翻译** | **15+** | 🟡 中 | 待修复 |
| 资源分类标签未翻译 | 3 | 🟡 中 | 待修复 |
| JavaScript Console Error | 1 | 🔴 高 | 待修复 |
| UI 优化建议 | 12 | 🟢 低 | 可选 |
| 功能增强建议 | 5 | 🟢 低 | 可选 |

**总计**: 105+ 个问题需要处理

### 优先级分布
- 🔴 **高优先级**: 14+ (影响核心用户体验)
- 🟡 **中优先级**: 7 (影响部分功能完整性)
- 🟢 **低优先级**: 17 (体验优化类)

### 紧急度排序
1. **资源分类系统不一致** - 两套 UI 逻辑冲突，可能导致功能失效
2. **设置面板多处未翻译** - 英文模式下设置功能不可用（10+ 处）
3. **科技树混合翻译** - 英文模式完全无法使用（36 项科技）
4. **解锁页面未翻译** - 英文模式下解锁信息不可读（15+ 段描述）
5. **成就系统部分翻译** - 英文模式下成就信息不完整（20+ 成就）
6. **HTML 图标错误** - 视觉错误影响专业度
7. **JavaScript Console Error** - 代码质量隐患
8. **资源分类标签** - 次要内容未翻译

---

## 🔗 相关文件索引

### 核心文件
- `index.html` - 主页面结构
- `js/i18n.js` - 国际化系统
- `js/bootstrap.js` - 启动流程
- `js/workers.js` - 工人管理 UI

### 样式文件
- `css/style.css` - 主样式
- `css/ascii-style.css` - ASCII 风格样式

### 配置文件
- `package.json` - npm 配置
- `Cargo.toml` - Rust 配置

---

## 🐛 新增：错误日志与异常

### JavaScript Console Errors

**发现时间**: 2026-03-19 13:33  
**错误信息**: `TypeError: Cannot set properties of null (setting 'value')`  
**位置**: `index.html:435:74`

```javascript
// 代码尝试访问不存在的元素
document.getElementById('language-select').value = selectedLanguage;
//                                    ^^^^^^^^^^^^^^^^
//                                    该元素在 HTML 中不存在
```

**影响**: 
- 语言切换时控制台报错
- 不影响核心功能但降低专业度
- 可能导致后续逻辑执行失败

**修复建议**:
```javascript
// 方案 1: 删除对不存在元素的引用
if (document.getElementById('language-select')) {
    document.getElementById('language-select').value = selectedLanguage;
}

// 方案 2: 在 header 中添加缺失的元素（如果设计需要）
```

### Auto-Save Log Frequency

**观察**: 自动保存每 15 秒执行一次（从日志可见）
```
[LOG] Game auto-saved at 01:33:48
[LOG] Game auto-saved at 01:34:03  ← 间隔 15 秒
```

**建议**: 确认这是否符合预期（通常在 `bootstrap.js` 中配置）

---

**报告生成时间**: 2026-03-19  
**测试环境**: Windows, Chrome/Chromium  
**测试工具**: Playwright Browser Automation  
**最后更新**: 2026-03-19 13:35
